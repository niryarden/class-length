# class-length

```
git clone git@github.com:niryarden/class-length.git

pip install -r requirements.txt

# macOS
export GITHUB_TOKEN="<Your_Github_Token>"
# Windows
$env:GITHUB_TOKEN="<Your_Github_Token>"

# only for macOS:
export OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES

```